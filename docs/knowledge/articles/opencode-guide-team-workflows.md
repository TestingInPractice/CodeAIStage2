---
type: article
source: "https://opencode.ai/docs/ru/"
source_file: "/Users/halapinvv/Documents/Agents/CodeAI/docs/opencode-guide/05-team-workflows.md"
imported: 2026-07-28
tags: [opencode, guide, documentation]
status: imported
---

# Командные процессы

# Командные процессы

**Глава 05**

---

## Базовый цикл: Plan → Spec → Build → Verify

1. Открыть задачу в **plan** и прояснить решение
2. Если меняется контракт, архитектура или продуктовый сценарий — обновить `docs/specs`
3. Переключиться в **build** и сделать минимальный рабочий slice
4. Проверить diff, тесты, поведение и сопутствующую документацию

## Когда оставаться в `plan`

| Ситуация | Почему не стоит сразу идти в build |
|----------|-----------------------------------|
| Архитектура неясна | Нужно сначала понять, в каком слое должно жить изменение |
| Задача затрагивает новые контракты | Нужно сформировать assumptions и acceptance criteria |
| Планируется широкий рефакторинг | Есть риск начать не с того места |

## Маршрутизация по ролям

**Изменение требований:** сначала `system-analyst` или `project-manager`, потом инженерная реализация.

**Работа с пайплайном и данными:** `data-engineer` как главный исполнитель.

## Specs-first development

- Меняется API contract — обновите spec
- Меняется data flow — обновите spec
- Появляется новый продуктовый сценарий — обновите spec
- Исправляется локальный баг без изменения intent — spec чаще всего не нужен

## Что должен получать новый инженер в первый день

1. Репозиторий с уже закоммиченными `AGENTS.md` и `opencode.json`
2. Короткая инструкция: как запустить `opencode`, где лежат specs и как работает `plan` vs `build`
3. Список project subagents и краткое объяснение, кого и когда использовать
4. Набор типовых промптов или custom commands для частых сценариев

## Модель зрелости внедрения

| Стадия | Что уже есть |
|--------|-------------|
| Stage 1 | `/init` и базовый `AGENTS.md` |
| Stage 2 | `opencode.json`, `instructions` и `docs/specs` |
| Stage 3 | Project-specific agents в `.opencode/agents` |
| Stage 4 | Curated skills и явная permission policy |
| Stage 5 | Повторяемые командные процессы, onboarding, operating model |

## Антипаттерны

- **Один гигантский prompt на всё** — не использовать слои specs, skills, commands
- **Никакой маршрутизации** — все задачи в один build-agent без ролей
- **Полное доверие build-mode** — почти не используется `plan`
- **Сверхусложнение в первый день** — десятки агентов и skills до понимания, какие нужны